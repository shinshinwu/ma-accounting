<% content_for :site_title do %>Edit Announcement<% end %>

<% content_for :breadcrumbs do %>
  <ol class="breadcrumb">
    <li><%= link_to 'All Announcements', announcements_path %></li>
    <li class="active">Edit Announcement</li>
  </ol>
<% end %>

<%= render partial: 'form', locals: {announcement: @announcement, path: announcement_path(@announcement), method: :put} %>
